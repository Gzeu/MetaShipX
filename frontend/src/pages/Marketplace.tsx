/**
 * @deprecated Use MarketplacePage.tsx (canonical). This file is kept only for
 * backward-compat imports; it will be deleted in v1.0.0.
 *
 * MarketplacePage.tsx is the single source of truth.
 * Logic components live in frontend/src/components/marketplace/.
 */
export { default } from './MarketplacePage';
